% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/z_read.R
\name{z_read}
\alias{z_read}
\title{z_read: Fast in-memory column picking from very large CSV}
\usage{
z_read(
  pattern,
  csv_path = "F:/UKB/总数据.csv",
  include_eid = TRUE,
  eid_name = "eid",
  n_max = Inf,
  threads = NULL,
  encoding = "UTF-8",
  ignore_case = TRUE
)
}
\arguments{
\item{pattern}{字符串或正则，用于匹配列名（如 \code{"41270"} 或 \code{"^(20002|20003|41270)"}）。}

\item{csv_path}{CSV 路径；默认 \code{"F:/UKB/总数据.csv"}。}

\item{include_eid}{是否强制包含 \code{eid} 列（默认 TRUE）。}

\item{eid_name}{\code{eid} 列名（默认 \code{"eid"}；若表头不同可改）。}

\item{n_max}{仅读取前 n 行预览；默认 \code{Inf}（全量）。}

\item{threads}{并行线程数；默认 \code{NULL} 表示用 (逻辑核数 - 1) 并在函数内临时设置。}

\item{encoding}{文件编码，默认 \code{"UTF-8"}。}

\item{ignore_case}{是否忽略大小写匹配列名，默认 TRUE。}
}
\value{
data.table（兼容 data.frame）
}
\description{
仅从超大 CSV 中按\strong{列名模式/正则}选择需要的列并读入内存（不生成中间文件）。
基于 data.table::fread 的列裁剪与多线程解析，速度快、内存小。
}
\examples{
\dontrun{
# eid + 所有包含 41270 的列
d <- ZJR::z_read("41270")

# 多个前缀
d <- ZJR::z_read("^(20002|20003|41270)")

# 预览前5万行
d <- ZJR::z_read("41270", n_max = 50000)

# 自定义 eid 列名
d <- ZJR::z_read("41270", eid_name = "eid_0_0")
}
}

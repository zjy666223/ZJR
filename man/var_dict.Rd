% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/var_dict.R
\name{var_dict}
\alias{var_dict}
\title{变量字典生成器（自动赋值并 View）}
\usage{
var_dict(
  df,
  max_cat_levels = 20,
  sample_n = 5,
  include_na_levels = FALSE,
  digits = 3,
  assign_name = "dict",
  view = TRUE
)
}
\arguments{
\item{df}{data.frame/tibble。}

\item{max_cat_levels}{整数阈值：唯一值个数 \<= \code{max_cat_levels} 时倾向判为分类变量；默认 20。}

\item{sample_n}{示例值数量（仅用于分类变量展示），默认 5。}

\item{include_na_levels}{是否在分类变量示例中包含 NA，默认 FALSE。}

\item{digits}{连续变量摘要保留小数位，默认 3。}

\item{assign_name}{赋值到全局环境的对象名，默认 "dict"。}

\item{view}{是否自动 \code{View()}，默认 TRUE。}
}
\value{
返回生成的字典 data.frame（并隐式赋值到全局环境）。
}
\description{
目标：在脚本里只需敲 \code{var_dict(d)}，即可等价于
\verb{dict <- var_dict(d); View(dict)}。
函数会把结果自动赋值到全局环境的 \code{dict} 对象，并在交互会话中自动 \code{View()}。
}
\examples{
\dontrun{
# 一键（最省事）：
var_dict(d)  # 等价于 dict <- var_dict(d); View(dict)

# 改名且不 View：
var_dict(d, assign_name = "mydict", view = FALSE)

# 仅返回值，不做副作用：
res <- var_dict(d, view = FALSE, assign_name = "tmp")
}
}

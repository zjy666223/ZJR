% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/drop_cols.R
\name{drop_cols}
\alias{drop_cols}
\title{删除或保留数据框中的列}
\usage{
drop_cols(data, cols, keep = FALSE, fuzzy = FALSE, ignore_missing = TRUE)
}
\arguments{
\item{data}{数据框或 tibble。}

\item{cols}{列名字符向量。}

\item{keep}{逻辑值，TRUE 表示只保留 cols（反选模式），FALSE 表示删除 cols（默认）。}

\item{fuzzy}{逻辑值，是否启用模糊匹配（默认 FALSE）。}

\item{ignore_missing}{逻辑值，是否忽略不存在的列（默认 TRUE）。}
}
\value{
删除或保留指定列后的数据框。
}
\description{
批量删除或保留数据框中的列。支持精确匹配或模糊匹配。
}
\examples{
# 删除列
df <- drop_cols(d, c("sex", "BMI_cat"))
只保留列（反选
df <- drop_cols(d, c("age", "PM2.5_per5"), keep = TRUE)
1
df <- data.frame(a = 1:3, b = 4:6, c = 7:9)
drop_cols(df, c("a", "c"))                  # 删除 a 和 c
drop_cols(df, c("a", "c"), keep = TRUE)      # 只保留 a 和 c
drop_cols(df, "b", fuzzy = TRUE)             # 模糊匹配列名
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recode_categoricals.R
\name{recode_categoricals}
\alias{recode_categoricals}
\title{批量将数据框中的分类变量编码为 0,1,...}
\usage{
recode_categoricals(
  data,
  cols = NULL,
  include_integer = FALSE,
  int_max_levels = 10,
  append = TRUE,
  suffix = "_num",
  start = 0,
  to_numeric = TRUE
)
}
\arguments{
\item{data}{数据框。}

\item{cols}{需要编码的列名字符向量；默认 \code{NULL} 表示自动识别。}

\item{include_integer}{逻辑值，是否把“看起来像分类”的整数列也一起编码。
仅当该列是整数型且唯一值个数 \verb{<= int_max_levels} 时视为分类。默认 \code{FALSE}。}

\item{int_max_levels}{识别整数列为分类的最大水平数阈值，默认 10。}

\item{append}{是否追加新列，见 \code{recode_categorical()}。默认 \code{TRUE}。}

\item{suffix}{新列后缀，默认 \code{"_num"}。}

\item{start}{起始编码，默认 0。}

\item{to_numeric}{是否输出数值型列，默认 \code{TRUE}。}
}
\value{
返回编码后的数据框。
}
\description{
自动识别数据框中的分类变量（字符/因子/逻辑），或按 \code{cols} 指定的列集合，
将其按水平顺序编码为 \verb{start, start+1, ...}。可选择是否仅识别“看起来像分类”的
整数列（唯一值数不超过 \code{int_max_levels}）。
}
\examples{
d <- data.frame(sex = c("M","F","M"), edu = factor(c("low","high","low")),
                grp = c(1L, 2L, 1L), x = 3.2)
# 自动识别字符/因子列
d1 <- recode_categoricals(d)
# 包含整数型（小水平数）一起编码
d2 <- recode_categoricals(d, include_integer = TRUE, int_max_levels = 5)
# 指定列名
d3 <- recode_categoricals(d, cols = c("sex","edu"), append = FALSE)
}

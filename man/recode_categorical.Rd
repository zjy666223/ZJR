% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recode_categorical.R
\name{recode_categorical}
\alias{recode_categorical}
\title{将分类变量重编码为 0,1,...}
\usage{
recode_categorical(
  data,
  colname,
  append = TRUE,
  suffix = "_num",
  start = 0,
  to_numeric = TRUE
)
}
\arguments{
\item{data}{数据框。}

\item{colname}{列名（字符串）。}

\item{append}{是否追加新列而不覆盖原列。若 \code{TRUE}，新列名为
\code{paste0(colname, suffix)}；若 \code{FALSE} 则覆盖原列。默认 \code{TRUE}。}

\item{suffix}{当 \code{append=TRUE} 时附加在新列名后的后缀，默认 \code{"_num"}。}

\item{start}{编码起点，默认 0。}

\item{to_numeric}{是否返回数值型（\code{TRUE}）或返回带数值标签的因子（\code{FALSE}）。}
}
\value{
返回数据框；指定列已被编码（覆盖或新增）。
}
\description{
对数据框中的单个分类变量执行有序数值化编码：按水平顺序映射到
\verb{start, start+1, ...}（默认从 0 开始）。字符列先转为因子；因子列保留其
原有水平顺序。NA 保持为 NA。
}
\examples{
d <- data.frame(sex = c("male","female","male"), edu = c("low","high","low"))
d <- recode_categorical(d, "sex")
d <- recode_categorical(d, "edu", start = 0, append = TRUE, suffix = "_id")
}

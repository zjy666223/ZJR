% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/z_rename.R
\name{z_rename}
\alias{z_rename}
\title{Rename columns by mapping or paired vectors (robust, base R).}
\usage{
z_rename(
  data,
  mapping = NULL,
  old = NULL,
  new = NULL,
  strict = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{data}{A data.frame/tibble/data.table; returned class matches input.}

\item{mapping}{Named character or list: names are old, values are new.
Example: c(old1 = "new1", old2 = "new2"). Ignored if \code{old}+\code{new} provided.}

\item{old}{Character vector of old names (alternative to \code{mapping}).}

\item{new}{Character vector of new names, same length and order as \code{old}.}

\item{strict}{logical. If TRUE, error on missing old names or duplicate new names.}

\item{quiet}{logical. If FALSE, print a concise summary of renames.}
}
\value{
The same object with columns renamed (same class as input).
}
\description{
Rename columns by mapping or paired vectors (robust, base R).
}
\examples{
df <- data.frame(a=1,b=2,c=3)
z_rename(df, mapping = c(b="beta", c="gamma"))
z_rename(df, old=c("a","b"), new=c("alpha","beta"))
}

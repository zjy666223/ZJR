% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inspect_df_vars.R
\name{inspect_df_vars}
\alias{inspect_df_vars}
\title{检查前20列数据结构}
\usage{
inspect_df_vars(
  df,
  cols = NULL,
  max_cat_levels = 20,
  max_show = 20,
  digits = 3,
  include_na_levels = FALSE,
  return_table = FALSE
)
}
\arguments{
\item{df}{data.frame/tibble。}

\item{cols}{字符向量，待检查的列名；若缺省，默认使用数据框的前 10 列。}

\item{max_cat_levels}{整数，判定分类变量的最大唯一值阈值（超过则不强制判为分类），默认 20。}

\item{max_show}{整数，当分类变量唯一值个数大于该阈值时，只展示前 \code{max_show} 个并在末尾标注总数，默认 20。}

\item{digits}{小数位数，连续变量统计量的格式化精度，默认 3。}

\item{include_na_levels}{逻辑值，是否在分类变量的唯一值中包含 NA，默认 FALSE。}

\item{return_table}{逻辑值，若为 TRUE，返回适合 \code{View()} 的\strong{长表}（不截断分类变量的水平，显示全部）；
为 FALSE（默认）时返回摘要 data.frame 并使用自定义 \code{print()} 竖排展示。}
}
\value{
\itemize{
\item 当 \code{return_table = FALSE}：一个 data.frame（列：\code{column}, \code{type}, \code{class}, \code{summary}），并带有类 \code{inspect_df_vars}，用于竖排打印。
\item 当 \code{return_table = TRUE}：一个\strong{长表} data.frame（列：\code{column}, \code{type}, \code{class}, \code{metric}, \code{value}）。
}
}
\description{
通用数据检查工具：给定数据框与列名，自动识别分类/连续变量并返回摘要；
同时提供自定义的 \code{print()} 方法以\strong{竖向块状}展示结果；
也可一键返回适合 \code{View()} 的长表（含分类变量的全部水平）。
}
\examples{
set.seed(1)
df <- data.frame(
  sex = sample(c("M","F"), 20, TRUE),
  age = sample(18:70, 20, TRUE),
  bmi = rnorm(20, 24, 3),
  edu = sample(c("High","Bachelor","Master"), 20, TRUE),
  stringsAsFactors = FALSE
)
# 摘要（竖排打印）
inspect_df_vars(df)
# 返回可 View 的长表（分类变量显示全部水平）
tbl <- inspect_df_vars(df, return_table = TRUE); head(tbl)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/z_forestplot.R
\name{z_forestplot_save}
\alias{z_forestplot_save}
\title{Save a forest plot to file (pdf/png)}
\usage{
# 1) 直接给数据表：函数内部会调用 z_forestplot() 再保存
z_forestplot_save("forest.pdf", data, width = 8, height = 6)

# 2) 先画图，再保存（支持位置参数或命名参数）
p <- z_forestplot(data, draw = TRUE)
z_forestplot_save("forest.pdf", plot = p)
z_forestplot_save("forest.pdf", p)  # 向下兼容：第二个无名参数识别为 plot

# 3) 保存 PNG
z_forestplot_save("forest.png", data, width = 8, height = 6, res = 300)
}
\arguments{
\item{filename}{output path; extension decides device ("pdf" or "png").}

\item{data}{Either a data.frame used by \code{z_forestplot()}, or NULL if using \verb{plot=}.
For backward-compatibility, if you pass a forestplot object as the
2nd unnamed argument, it will be treated as \code{plot}.}

\item{width, height}{numeric; in inches.}

\item{res}{integer; dpi for PNG (ignored for PDF). Default 300.}

\item{bg}{background color. Default "white".}

\item{plot}{Optional. A forestplot object returned by \code{z_forestplot(...)}.}

\item{...}{further arguments passed to \code{z_forestplot()} when \code{data} is provided.}
}
\value{
Invisibly returns the forestplot object.
}
\description{
Convenience wrapper to export a forest plot produced by \code{z_forestplot()}.
}
